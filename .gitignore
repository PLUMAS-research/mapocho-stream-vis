# === Dependencias / entornos (regenerables) ===
node_modules/
.venv/
__pycache__/
*.pyc

# === Build de la app React ===
build/
coverage/

# === Variables de entorno (no versionar credenciales) ===
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# === Datos (no se versiona ningún dato) ===
# Todo se regenera desde las fuentes externas (viajes + feed GTFS) con
# preparar_datos/generar_todo.sh; ver preparar_datos/README.md.
src/json/
src/particulasPrecalculadas.json
preparar_datos/.gtfs_cache/

# === Instrucciones locales de asistentes (no se versionan) ===
CLAUDE.md
.claude/

# === Misc ===
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
